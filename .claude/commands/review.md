---
description: Utilise explicitement le sous-agent revue pour relire les changements récents
---

Utilise explicitement le sous-agent `revue` (voir `.claude/agents/revue.md`) pour traiter la demande suivante, pour relire uniquement les changements récents (git diff), sans modifier de fichier :

$ARGUMENTS
