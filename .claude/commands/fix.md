---
description: Utilise explicitement le sous-agent fix-rapide pour un correctif minimal et ciblé
---

Utilise explicitement le sous-agent `fix-rapide` (voir `.claude/agents/fix-rapide.md`) pour traiter la demande suivante. Reste sur une correction minimale et ciblée :

$ARGUMENTS
