---
description: Utilise explicitement le sous-agent tests pour écrire/exécuter des tests
---

Utilise explicitement le sous-agent `tests` (voir `.claude/agents/tests.md`) pour traiter la demande suivante. Priorise des tests ciblés plutôt que la suite complète :

$ARGUMENTS
