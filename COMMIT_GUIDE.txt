== COMMIT GUIDE — Auto-Refresh-and-Retry for YouTube Cookies ==

Changed Files:
  MODIFIED: backend/services/youtube_service.py
    - Added auto-refresh-and-retry logic when yt-dlp fails with "Sign in..." error
    - Detects YOUTUBE_ENABLE_AUTO_REFRESH env var
    - Launches refresh_youtube_cookies.py script
    - Parses WROTE diagnostic line for safe logging
    - Retries yt-dlp with refreshed cookies (same job, same IP)

  ADDED: scripts/refresh.env.example
    - Template environment file for auto-refresh configuration
    - Variables: YOUTUBE_ENABLE_AUTO_REFRESH, YOUTUBE_BROWSER_PROFILE_DIR, etc.

  ADDED: scripts/refresh_oneoff.sh
    - Bash wrapper to run refresher one-off
    - Parses env, executes refresher, validates cookies with yt-dlp canary
    - Usage: bash scripts/refresh_oneoff.sh

  ADDED: scripts/test_autorefresh_logic.py
    - Validation tests for auto-refresh setup
    - Tests: script existence, compilation, WROTE line parsing
    - Usage: python3 scripts/test_autorefresh_logic.py

  ADDED: scripts/test_autorefresh_e2e.sh
    - End-to-end test: refresh → parse → test with yt-dlp canary
    - Simulates full auto-refresh-and-retry flow
    - Usage: bash scripts/test_autorefresh_e2e.sh

  ADDED: scripts/AUTOREFRESH_DEPLOYMENT.md
    - Complete French deployment guide
    - Setup, testing, troubleshooting, FAQ
    - Installation steps for production

  ADDED: AUTOREFRESH_CHANGES.md
    - Summary of changes in French
    - Activation steps and security notes

Commit Message:
  feat: auto-refresh-and-retry for YouTube cookies on 'Sign in' error
  
  - Backend now detects 'Sign in to confirm...' from yt-dlp
  - Automatically launches Playwright refresher when YOUTUBE_ENABLE_AUTO_REFRESH=true
  - Retries yt-dlp with refreshed cookies in same job (guarantees IP affinity)
  - Logs only safe diagnostics: file size + SHA256 (no cookie contents)
  - Includes bash wrapper, validation tests, and deployment guide (French)
  
  Resolves recurring "Sign in" errors by automating cookie refresh without manual intervention.

Steps:
  1. Review changes: git diff HEAD
  2. Run tests locally:
     python3 scripts/test_autorefresh_logic.py
     bash scripts/test_autorefresh_e2e.sh  (requires browser profile)
  3. Commit:
     git add scripts/refresh.env.example scripts/refresh_oneoff.sh \
             scripts/test_autorefresh_logic.py scripts/test_autorefresh_e2e.sh \
             scripts/AUTOREFRESH_DEPLOYMENT.md backend/services/youtube_service.py \
             AUTOREFRESH_CHANGES.md
     git commit -m "feat: auto-refresh-and-retry for YouTube cookies on 'Sign in' error"
  4. Push:
     git push origin main
  5. Deploy:
     Set YOUTUBE_ENABLE_AUTO_REFRESH=true + profile path in production env

Testing Before Production:
  - Local: bash scripts/test_autorefresh_e2e.sh
  - Staging: set YOUTUBE_ENABLE_AUTO_REFRESH=true, monitor logs for [YTC AUTOREFRESH]
  - Verify: no persistent "Sign in" errors; successful auto-refresh-and-retry
